task:
  id: "fix-auth-bypass_1"
  desc: "Fix the authentication bypass when the password field is empty"
  graders:
    - type: deterministic_tests
      required:
        - test_empty_pw_rejected.py
        - test_null_pw_rejected.py
    - type: state_check
      expect:
        security_logs: {event_type: "auth_blocked"}
