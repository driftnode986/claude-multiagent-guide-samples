[project]
name = "ch07-knowledge-base-mcp"
version = "0.1.0"
description = "Minimal FastMCP server embodying the 6 ACI principles"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.12",
    "pydantic>=2.0",
]

[project.scripts]
ch07-server = "server:mcp.run"
