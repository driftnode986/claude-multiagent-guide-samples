{
  "name": "Grep",
  "parameters": {
    "output_mode": {
      "enum": ["content", "files_with_matches", "count"],
      "description": "content: show matching lines. files_with_matches: file paths only. count: match counts only"
    },
    "head_limit": {
      "type": "number",
      "description": "Limit output to first N lines"
    }
  }
}
